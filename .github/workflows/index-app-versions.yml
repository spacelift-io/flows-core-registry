# This workflow is not meant to be run directly.
# It is called by app repos to index app versions after they are uploaded.
# It is triggered by the `workflow_call` event.
name: Index app versions

on:
  workflow_call:

jobs:
  index:
    name: Index app versions
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Index app versions
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.FLOWS_REGISTRY_R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.FLOWS_REGISTRY_R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: auto
          R2_BUCKET: registry-useflows-com
          R2_ENDPOINT_URL: https://5f0d4105431f10a4af1e1f0266f26a97.r2.cloudflarestorage.com
        run: |
          curl -o index-app-versions.cjs https://raw.githubusercontent.com/spacelift-flows-apps/flows-core-registry/refs/heads/main/.github/scripts/index-app-versions.cjs
          node index-app-versions.cjs
